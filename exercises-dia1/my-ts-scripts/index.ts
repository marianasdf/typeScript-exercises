import * as length from './length';
import * as mass from './mass';
import * as capacity from './capacity';

console.log(length.convert(100, 'm', 'mm'));
console.log(mass.convert(100, 'g', 'mg'));
console.log(capacity.convert(100, 'kl', 'l'));